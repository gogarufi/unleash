TODO: Client blindly trusts the server's data entries to be unique (UID: street, city, postal number). Should handle duplicates gracefully.

TODO: Complex state managemnt for the address search - A11y + keyboard navigation + mouse interactions. Possibly can be simplified.
